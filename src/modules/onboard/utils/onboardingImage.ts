export const getOnboardingImage = (index: number): string => {
  const imageUrl = `https://github.com/varchasvipandey/media/blob/main/productiview-onboarding-images/${index}.png?raw=true`;
  return imageUrl;
};

export const totalOnboardingImages = 6;
